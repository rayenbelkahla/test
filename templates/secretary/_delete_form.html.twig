<form method="post" action="{{ path('app_secretary_delete', {'id': secretary.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ secretary.id) }}">
    <button class="btn">Delete</button>
</form>
