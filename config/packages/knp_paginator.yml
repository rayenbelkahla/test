knp_paginator:
    page_range: 5               # Number of pages displayed in the pagination menu (optional)
    default_options:
        page_name: page         # Page parameter name
        sort_field_name: sort   # Sort field parameter name
        sort_direction_name: direction   # Sort direction parameter name
        distinct: true          # Ensure distinct results when paginating (optional)
